<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="${namespace}" >
  <resultMap id="BaseResultMap" type="${domainType}" >
    <id column="${idColumn}" property="${idProperty}" jdbcType="${idType}" />
    <!-- <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="label_id" property="labelId" jdbcType="INTEGER" /> -->
  </resultMap>
  <sql id="Example_Where_Clause" >
    <where >
      <foreach collection="oredCriteria" item="criteria" separator="or" >
        <if test="criteria.valid" >
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion" >
              <choose >
                <when test="criterion.noValue" >
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue" >
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue" >
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue" >
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Example_Where_Clause" >
    <where >
      <foreach collection="example.oredCriteria" item="criteria" separator="or" >
        <if test="criteria.valid" >
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion" >
              <choose >
                <when test="criterion.noValue" >
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue" >
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue" >
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue" >
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List" >
    ${Base_Column_List}
  </sql>
  <select id="selectByExample" resultMap="BaseResultMap" parameterType="${exampleType}" >
    select
    <if test="distinct" >
      distinct
    </if>
    <include refid="Base_Column_List" />
    from ${tableName}
    <if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null" >
      order by ${orderByClause}
    </if>
  </select>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="${IdType}" >
    select 
    <include refid="Base_Column_List" />
    from ${tableName}
    where ${idColumn} = #{${idProperty}}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="${IdType}" >
    delete from ${tableName}
    where id = #{${idProperty}}
  </delete>
  <delete id="deleteByExample" parameterType="${exampleType}" >
    delete from ${tableName}
    <if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
  </delete>
  <insert id="insert" parameterType="${domainType}" >
    ${insertSql}
  </insert>
  <insert id="insertSelective" parameterType="${domainType}" >
   ${insertSelectiveSql}
  </insert>
  <select id="countByExample" parameterType="${exampleType}" resultType="java.lang.Integer" >
    select count(*) from ${tableName}
    <if test="_parameter != null" >
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  <update id="updateByExampleSelective" parameterType="map" >
    ${updateByExampleSelectiveSql}
  </update>
  <update id="updateByExample" parameterType="map" >
    ${updateByExampleSql}
  </update>
  <update id="updateByPrimaryKeySelective" parameterType="${domainType}" >
    ${updateByPrimaryKeySelectiveSql}
  </update>
  <update id="updateByPrimaryKey" parameterType="${domainType}" >
    ${updateByPrimaryKeySql}
  </update>
</mapper>